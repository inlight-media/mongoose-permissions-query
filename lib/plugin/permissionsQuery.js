export default function applyPlugin( schema ) {
  schema.statics.permissionsQuery = permissionsQuery
}

function permissionsQuery(permissionsOptions, callback) {

  // if (){
  //   let error =
  //   callback(error)
  // }
}
